[package]
name = "rostop-core"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
description = "Pure-logic core for rostop: stats, registry, rendering helpers. No ROS2 dependency."

[dependencies]
thiserror.workspace = true

[dev-dependencies]
# kept minimal — pure-logic tests
